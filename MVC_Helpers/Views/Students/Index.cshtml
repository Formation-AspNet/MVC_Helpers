@model IEnumerable<MVC_Helpers.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Group.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StringData)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IntData)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BoolData)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTimeData)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Group.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StringData)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IntData)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BoolData)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateTimeData)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentId })
        </td>
    </tr>
}

</table>
